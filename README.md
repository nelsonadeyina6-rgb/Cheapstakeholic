# CheapStakeHolic — Starter Project
This is a starter project for "CheapStakeHolic" (purple & black themed online store).
Files include a backend (server.js), public frontend (public/), admin UI (admin/), and an example .env file.
Fill .env with your keys, run `npm install`, then `node server.js` to start locally.
